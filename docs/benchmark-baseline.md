# Speech Benchmark Report

No benchmark run has been committed yet.

Run:

```bash
node scripts/benchmark-speech.mjs --dataset scripts/benchmark/smoke-set.json --out docs/benchmark-baseline.md
```

Expected smoke-set metrics:

- exact/near transcript match
- p50 and p95 latency
- average score
